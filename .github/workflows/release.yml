name: Release

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  release:
    uses: karoosoftware/github-actions-workflows/.github/workflows/terraform-module-release.yml@main
