name: Terraform

on:
  push:
    branches:
      - develop
  pull_request:
    branches:
      - main

jobs:
  validate:
    uses: karoosoftware/github-actions-workflows/.github/workflows/terraform-module-validate.yml@main
